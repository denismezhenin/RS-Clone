import { IState } from '../data/types';

const state: IState = {
  authToken: '',
  id: '',
  boardId: '',
  name: '',
  pageLoaded: false,
  selectedUserId: '',
  selectedUserIdToTask: '',
  membersPage: 1,
  members: [],
};

export default state;
